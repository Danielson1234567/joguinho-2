body {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-family: Arial, sans-serif;
    background-color: #f0f0f0;
    margin-top: 50px;
}

h1 {
    color: #333;
}

/* Estilo para o elemento onde o jogo é desenhado */
#gameCanvas {
    border: 5px solid #34495e;
    display: block; /* Garante que não haja espaço extra abaixo do canvas */
}

/* Estilo para o placar */
.score-board {
    margin-top: 15px;
    font-size: 1.5em;
    font-weight: bold;
    color: #2c3e50;
}

#score {
    color: red;
}